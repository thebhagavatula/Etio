# One SDK and one screen for the whole UI suite, pinned so a machine with a different
# compileSdk does not silently test a different Android — and so the layout under test
# is a modern phone rather than Robolectric's 320x470 default, which squeezes every
# row until taps land on the wrong thing.
sdk=34
qualifiers=w411dp-h891dp-xhdpi
