<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- The pre-Compose splash window only. The designed splash that follows it is
         Compose and follows the app's own theme choice; this one can only follow the
         system, because the choice lives in DataStore and is not readable from the
         resource system before the first frame. -->
    <color name="etio_splash_bg">#FF17120B</color>
    <color name="etio_splash_icon_bg">#FF17120B</color>
</resources>
